import { MenuType } from "./menu-type";

/**
 * The menu type of the Onshape tab the app is open in.
 */
export enum AppType {
    PART_STUDIO = MenuType.PART_STUDIO,
    ASSEMBLY = MenuType.ASSEMBLY
}
