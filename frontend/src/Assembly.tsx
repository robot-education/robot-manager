import React from "react";
import material from "@mui/material";

function AssemblyApp(): JSX.Element {
    return (
        <material.AppBar position="fixed">
            <material.Toolbar>Auto assembly</material.Toolbar>
        </material.AppBar>
    );
}